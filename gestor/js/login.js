/* Entrada real: desafio por WhatsApp. HTML de demonstração usa link separado. */
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-pref]').forEach(el=>el.addEventListener('change',()=>Preferences.set({[el.dataset.pref]:el.value})));const form=document.querySelector('#auth-form');if(!form)return;let challenge='';form.addEventListener('submit',async e=>{e.preventDefault();let button=form.querySelector('button[type=submit]'),err=document.querySelector('#auth-error');button.disabled=true;err.hidden=true;try{const data={phone:form.phone.value,code:form.code?.value||'',challenge};const r=await fetch('auth.php?action='+(challenge?'verify':'request'),{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':window.AUTH_CSRF},credentials:'same-origin',body:JSON.stringify(data)});const result=await r.json();if(!r.ok)throw Error(result.error||'Não foi possível validar o acesso.');if(challenge){location.href='index.php';return;}challenge=result.challenge;document.querySelector('#code-field').hidden=false;form.code.disabled=false;form.code.required=true;form.phone.readOnly=true;button.textContent='Validar código e entrar';document.querySelector('#auth-note').textContent='Se o número estiver autorizado, o código será enviado pelo WhatsApp. Expira em 5 minutos.';form.code.focus();}catch(error){err.textContent=error.message;err.hidden=false;err.focus();}finally{button.disabled=false;}});});